# Adding an event

Copy an existing `.md` file in this directory. Change the front matter and body,
then push the file. The Events page is generated automatically.

Required fields: `title` and `date`. Optional fields: `label`, `image`, `link`,
and `link_text`. Images belong in `assets/images/`.

