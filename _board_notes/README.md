# Adding board notes

1. Put the PDF or DOCX file in `assets/documents/`.
2. Copy an existing `.md` file in this directory and update its `title`, `date`,
   `format`, and `document` path.
3. Push both files. The newest note appears first automatically.

